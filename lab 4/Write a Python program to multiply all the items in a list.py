# Sample list
numbers = [2, 3, 4, 5]

# Initialize result to 1 (neutral element for multiplication)
result = 1

# Multiply each item in the list
for num in numbers:
    result *= num

# Print the result
print("Product of all items:", result)
