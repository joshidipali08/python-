# Program to check if a string contains only digits

# Input from the user
string = input("Enter a string: ")

# Check using isdigit() method
if string.isdigit():
    print("The string contains only digits.")
else:
    print("The string does not contain only digits.")
